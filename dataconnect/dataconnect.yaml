specVersion: "v1"
serviceId: "quantnex"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quantnex-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
